import ContactPage from "@/components/contact/ContactPage";

export const metadata = {
  title: "Contact Us | Best Tour Drivers in Sri Lanka",
  description:
    "Get in touch with Best Tour Drivers in Sri Lanka for private tours, WhatsApp support, and worldwide agent contacts.",
};

const page = () => {
  return <ContactPage />;
};

export default page;
